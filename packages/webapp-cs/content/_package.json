{
  "name": "{{name}}",
  "version": "0.1.0",
  "description": "",
  "scripts": {
    "boot": "(cd client && npm i) && (cd server && npm i)",
    "start": "cd server && npm start",
    "build": "(cd client && npm run build) && (cd server && npm run build)"
  },
  "keywords": [],
  "author": "{{author}}",
  "license": "ISC"
}
